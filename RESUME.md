# 简历素材与面试话术

## 📝 项目经历(可直接粘贴到简历)

### Java 面试知识库智能问答系统(RAG 检索增强生成)

**技术栈**:Python · FastAPI · ChromaDB · BGE 中文向量模型 · BM25 · DeepSeek API · Docker

- 设计并实现基于 RAG 架构的文档问答系统:文档解析切分 → 向量化入库 → 混合检索 → 大模型生成,完整链路覆盖知识库问答全流程
- **混合检索优化**:BM25 关键词检索与 BGE 向量语义检索通过 RRF 排名融合互补,检索 Top-1 准确率由 33% 提升至 100%(3/3 测试用例全部命中)
- **检索质量调优**:实现 jieba 中文分词 + 停用词过滤 + 索引结构优化(标题与正文联合索引),解决专有名词(如 HashMap)检索丢失问题
- **防幻觉设计**:提示词强制基于检索资料回答并标注 [参考资料N] 出处,资料不足时明确声明,保证回答可溯源
- 使用 FastAPI 封装 REST API(/api/ask),内置 Web 问答页面,支持 Docker 一键部署

---

## 🎤 面试话术(5分钟项目讲解)

### 开场 30 秒(背景与定位)

> 我做的是一个基于 RAG 技术的 Java 面试知识库问答系统。背景是秋招复习时发现,面试题资料分散、直接问大模型答案不可靠还容易过时,所以我做了一个"先检索资料、再生成回答"的系统:内置 20 道高频 Java 面试题,提问后返回带出处标注的回答。

### 技术链路 2 分钟(讲清楚做了什么)

> 完整链路分四步:
> 1. **切分**:PDF/Markdown 按标题结构切块,超长块二次切分并做 50 字重叠,过滤杂质;
> 2. **向量化**:用 bge-small-zh 中文嵌入模型把文本转成 384 维向量,存进 ChromaDB;
> 3. **混合检索**:向量检索(语义匹配)+ BM25(关键词匹配),用 RRF 按排名融合;
> 4. **生成**:把问题+检索到的资料拼成 prompt 发给 DeepSeek,强制基于资料回答并标注引用。

### 技术难点 1 分钟(讲你解决的 bug)

> 最典型的问题:测试时发现"HashMap 底层怎么实现"检索不到 HashMap 那道题,排查发现 BM25 索引只存了正文、标题里的专有名词丢了。我把标题和正文合并索引后解决,Top-1 命中从 33% 提到 100%。这个排查过程让我理解了数据管线的每个环节都会影响检索质量。

### 结果与反思 30 秒

> 3 个测试用例全部 Top-1 命中,回答带出处可溯源。后续计划:接入重排序模型(bge-reranker)进一步提升召回精度、扩充知识库规模、支持多轮对话。

---

## ❓ 面试官可能的追问与参考回答

**Q: 为什么用混合检索而不是纯向量检索?**
> 纯向量检索对长文本相似度会稀释(我们的测试中正确题只有 0.5 左右的相似度),BM25 精确命中关键词但不懂语义,两者互补;RRF 只看排名融合,免疫两种算法打分尺度不同的问题。

**Q: 切块大小怎么定的?为什么 400 字?**
> 权衡检索精度和上下文成本:块太小语义不完整,块太大检索不精准、浪费 token。400 字 + 50 字重叠是中文场景的常用经验值,并设计了二次切分和标题感知切分,可配置调参。

**Q: 怎么防止大模型瞎编(幻觉)?**
> 三层:1) 提示词强制"只基于资料回答,资料不足要明说";2) 要求回答标注 [参考资料N] 实现可溯源;3) 温度设 0.3 降低自由发挥。企业级方案还会加"检索不到就拒绝回答"的兜底。

**Q: 向量模型为什么选 bge-small-zh?**
> 中文效果对标 OpenAI embedding,但免费、可本地部署,100MB 小体积适合个人项目;归一化向量后用点积等价余弦相似度,计算快。

**Q: 如果知识库有 100 万条文档,系统哪里会瓶颈?**
> 1) 向量检索:ChromaDB 单机百万级可用,更大需要 Elasticsearch 或 Milvus;2) BM25 内存索引:可换 ES 的 BM25 实现;3) 嵌入模型可改 API 化部署 + 缓存;4) LLM 调用加缓存和限流。

**Q: 多用户并发怎么办?**
> FastAPI 异步框架天然支持;检索服务启动时单例加载(模型加载很慢,不能每请求加载);LLM 调用是 IO 密集,靠协程并发;生产级加 Redis 缓存相同问题、限流、队列削峰。

---

## ✅ 简历自查清单

- [ ] 项目经历已量化(100% Top-1 命中、3 个测试用例)
- [ ] 技术栈关键词覆盖 JD(检索增强生成 RAG、混合检索、向量数据库、Prompt 工程、FastAPI)
- [ ] GitHub 仓库完整:README + 运行说明 + 提交记录
- [ ] 能 5 分钟脱稿讲完整链路
- [ ] 能答出上述 6 个追问
- [ ] (可选)部署上线拿到公网 demo 链接
